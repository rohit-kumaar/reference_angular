import { ValueChangeDirective } from './value-change.directive';

describe('ValueChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueChangeDirective();
    expect(directive).toBeTruthy();
  });
});
